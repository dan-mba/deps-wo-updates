{
  "name": "deps-wo-updates",
  "version": "1.0.0",
  "description": "Check package.json for dependencies where latest version is old",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "action-build": "ncc build src/action-index.ts -o dist --source-map",
    "new-action-build" : "esbuild  src/action-index.ts --bundle --outfile=dist/index.js --format=esm --platform=node --target=node20.0.0",
    "start": "node build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.10",
    "@typescript-eslint/eslint-plugin": "^7.0.1",
    "@typescript-eslint/parser": "^7.0.1",
    "@vercel/ncc": "^0.38.1",
    "esbuild": "^0.20.1",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "console-table-printer": "^2.12.0"
  }
}
